pipeline:
  build:
    image: node
    commands:
      - npm run install
      - npm run generate

  publish:
    gh_pages:
      source: dist
      remote: origin
      branch: master
    when:
      status: success
      branch: dev
