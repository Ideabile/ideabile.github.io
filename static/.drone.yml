pipeline:
  build:
    image: node
    commands:
      - npm run install
      - npm run generate
    when:
      status: success
      branch: dev
  publish:
    gh_pages:
      source: dist
      remote: origin
      branch: master
