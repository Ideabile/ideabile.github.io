pipeline:
  build:
    image: node:latest
    commands:
      - npm run install
      - npm run generate
  publish:
    gh_pages:
      source: dist
      remote: origin
      branch: master
    when:
      status: success
      branch: dev
